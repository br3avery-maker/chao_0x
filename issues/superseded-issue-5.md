# Supersedes Issue 5

Issue #5 was created when the repo still assumed a Base/EVM-first economy.

That assumption is wrong.

Replace with:

- native CHAO balance model
- native account-chain blocks
- native asset release blocks
- native burn and stake blocks
- native reward and challenge blocks
- bridges only after the native protocol exists
